package javafun;

public class EditorShortcomings {

    public static void main(String[] args) {

        // don't close the streams debugging!

        // try debugging a lambda expression

        // doesn't recognise that 'return' is unnecessary on ambiguous calls

        // have to be careful executing terminal operations when debugging streams (not really a shortcoming, just need to be aware)
    }
}
